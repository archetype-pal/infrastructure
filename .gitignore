env_file
# Compose interpolation vars (image pins, published ports) — host-specific.
.env
*.log*
backups/postgres-upgrade/

# Runtime TLS material (certbot-managed); keep the placeholder only
certs/*
!certs/.keep

# Uploaded media / runtime storage
storage/
